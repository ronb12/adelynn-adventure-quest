import UIKit

/// Game Center leaderboards from the native title screen (Scores tab).
enum GameCenterAuth {
    static func start(rootViewController: UIViewController?) {}

    static func presentLeaderboards(from viewController: UIViewController?) {}
}
